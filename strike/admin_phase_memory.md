# Admin Phase Memory

- [x] Option 2 left on the table after this backend hardening slice: standardize deterministic admin mutation error envelopes and request-id-backed error codes across the remaining privileged `/api/admin/*` routes once the current phase is closed.
- [x] Option 2 left on the table after this canonical admin consolidation slice: replace remaining `window.prompt` and `window.alert` approval and reason capture in `UsersSection`, `FeatureFlagsSection`, and `WorkspacesSection` with audited first-class dialogs.
- [x] Option 2 left on the table after this admin action-safety slice: remove or backend-wire the remaining static operator panels in `AdminApp` such as AI monitoring and settings so the canonical admin console does not present non-authoritative operational state.
- [x] Option 2 left on the table after this canonical-surface cleanup slice: publish real backend contracts for admin runtime settings and AI-ops telemetry before reintroducing those panels to the canonical admin nav.
- [x] Option 2 left on the table after this restored-runtime slice: replace the remaining static top-bar environment chrome and dead quick actions in `AdminApp` such as the fixed prod badge and `View error traces` button with runtime-settings-backed values or remove them.
- [x] Option 2 left on the table after this admin-shell cleanup slice: replace the dead `Export log` control in `LogsSection` with a real audit export path or remove it until backend export contracts exist.
- [x] Option 2 left on the table after this audit-log contract slice: remove the remaining garbled icon/text mojibake in the canonical admin UI and replace it with ASCII-safe copy or proper icon components so operators do not see corrupted chrome.
- [x] Option 2 left on the table after this admin copy-hygiene slice: replace the remaining emoji-first controls in high-risk operator surfaces like `UsersSection` and `WorkspacesSection` with a consistent icon system or ASCII-safe labels.
- [x] Option 2 left on the table after this admin mutation-envelope slice: bring unauthenticated admin invite validation and redeem flows onto the same structured response contract and request-id trace model as the privileged admin routes.
- [x] Option 2 left on the table after this invite-contract slice: add forensic audit coverage for failed invite validation and redeem attempts so admin onboarding incidents are reconstructable beyond request-level traces.
- [x] Option 2 left on the table after this invite-forensics slice: emit `x-request-id` headers across the `/api/admin/*` surface and surface those trace IDs in operator-facing admin client failures.
- [x] Option 2 left on the table after this request-id propagation slice: extend the same traceability contract to the non-admin control-plane dependencies that the admin console calls directly, especially autonomy and companion endpoints, so cross-surface incidents can be followed with a single request trail.
- [x] Option 2 left on the table after this cross-surface traceability slice: normalize companion and autonomy error bodies to the same structured `error_type` and request-scoped error-code contract used by `/api/admin/*`, so non-browser callers can debug failures without relying on headers alone.
- [x] Option 2 left on the table after this companion-autonomy error-contract slice: align successful companion and autonomy mutation responses to include the same explicit `result_state`, `message`, and `request_id` metadata as privileged admin mutations, so operator tooling does not need per-surface response branching.
- [ ] Option 2 left on the table after this companion-autonomy success-envelope slice: add route-level parity tests for the remaining companion and autonomy mutation endpoints so every active control-plane mutation contract is locked instead of inferred from representative dismiss/apply coverage.
